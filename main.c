// main.c
#include <stdio.h>

int addTwoNums(int first, int second) {
	return first + second;
}

int main() {
	int first = 8;
	int second = ;
	printf("%d + %d = %d\n", first, second, addTwoNums(first, second));
	return 0;
}
